// Matches template.json Item > templates > "health" (used by "class"), which declares no fields.
// Function, not object, for consistency with the other template modules (see base.mjs).
export function HealthFields() {
    return {};
}
